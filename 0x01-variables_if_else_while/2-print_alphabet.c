#include <stdio.h>
#include <stdlib.h>

/**
 * main - returns alphabets
 *
 * Returns: Always 0 (Success
 */
int main(void)
{
	int ch = 'a';

	while (char <= '2')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
	
